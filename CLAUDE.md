# CLAUDE.md

このファイルは、Claude Code / AI コーディング支援ツールが本研究リポジトリを扱う際の作業ルール・研究背景・実行手順をまとめたものです。

## 1. 研究概要

本研究は、5G と Wi-Fi が混在する異種無線ネットワーク環境において、端末ごとの QoE（Quality of Experience）を考慮した基地局選択・切り替え手法を検討する。

### 目的

ns-3 上に実環境に近い検証環境を構築し、基地局切り替えが通信品質に与える影響を分析する。最終的には、動的環境に適した割り当て手法、特に深層強化学習（DRL）に基づく基地局選択手法の設計・実装・評価を行う。

### 研究上の重要ポイント

- 端末満足度はスループット（TP）または RTT に基づいて算出する。
- 全体最適化の指標として、端末満足度の調和平均を用いる。
- 切り替え対象端末以外の通信品質にも影響が及ぶため、局所改善ではなく全体指標を重視する。

## 2. 技術スタック

### 固定環境

- ns-3: `3.44`
- 言語:
  - C++: ns-3 シミュレーション本体
  - Python: ログ解析、グラフ作成、DRL 学習・評価（まだ実装前）
- 想定 OS: `<WSL version>`
- コンパイラ: `<g++ version>`
- Python: `<Python version>`

### 重要な制約

- ns-3 のバージョンは原則として `3.44` から変更しない。
- 外部ライブラリを追加する場合は、理由・用途・再現手順を README または `docs/` に記録する。
- 実験結果を変更する可能性があるパラメータ変更は、必ずログまたは設定ファイルに残す。

---

## 3. 想定リポジトリ構成

実際の構成と異なる場合は、現在の構成を優先してこの節を更新する。

```text
.
├── CLAUDE.md
├── README.md
├── scratch/
│   └── qoe_selection_sim.cc          # ns-3 シミュレーション本体
├── src/
│   └── qoe-selection/                # 独自モジュールを作る場合
├── configs/
│   ├── default.yaml                  # 基本実験条件
│   ├── apps.yaml                     # アプリ別要求 TP / RTT
│   └── scenarios/                    # 端末数・配置・負荷条件別設定
├── scripts/
│   ├── run_sim.py                    # ns-3 実験実行ラッパ
│   ├── parse_flowmonitor.py          # FlowMonitor 出力解析
│   ├── calc_satisfaction.py          # 端末満足度計算
│   └── plot_results.py               # グラフ生成
├── rl/
│   ├── env.py                        # DRL 用環境ラッパ
│   ├── train.py                      # 学習
│   ├── evaluate.py                   # 評価
│   └── agents/                       # DQN / PPO など
├── data/
│   ├── raw/                          # 生ログ
│   ├── processed/                    # 加工済みデータ
│   └── results/                      # 実験結果
├── figures/
│   └──                              # 論文・発表用図
└── tests/
    ├── test_satisfaction.py
    └── test_log_parser.py
```

---

## 4. シミュレーション前提

### ネットワーク構成

- 基地局数: 3 台
  - 5G gNB: 1 台
  - Wi-Fi AP: 2 台
- 端末数: 50 台以上
- 検査用端末: 各基地局に 1 台ずつ、計 3 台
- アプリケーション:
  - ブラウザ閲覧
  - 動画視聴
  - 音声通話
  - オンラインゲーム
- 基本実行時間: 60 秒 （未確定）
- 基本サイクル: 6 秒間隔、合計 10 サイクル （未確定）

### 無線区間

- 5G: UE - gNB
- Wi-Fi: STA - AP
- バックホール区間は PointToPoint リンクで構成する。

---

## 5. QoE / 端末満足度の定義

端末 `i` の満足度 `S_i` は、使用アプリケーションが TP 重視か RTT 重視かで計算式を切り替える。

### TP 重視アプリ

対象例:

- ブラウザ閲覧
- 動画視聴

```text
S_i = TP_link / TP_need
```

### RTT 重視アプリ

対象例:

- 音声通話
- オンラインゲーム

```text
S_i = RTT_need / RTT_link
```

### アプリ別要求値

| アプリ | 指標 | 必要 TP | 必要 RTT |
|---|---:|---:|---:|
| ブラウザ閲覧 | TP | 2.3 Mbps | - |
| 動画視聴 | TP | 8.0 Mbps | - |
| 音声通話 | RTT | - | 100 ms |
| オンラインゲーム | RTT | - | 40 ms |

### 全体評価指標

端末満足度の調和平均を最大化対象とする。

```text
H = n / sum(1 / S_i)
```

実装上の注意:

- `S_i <= 0` が発生しないようにする。
- 分母が 0 または異常値になる場合は、ログに警告を出す。

---

### 実装上の注意

- 分類結果は各サイクルごとにログへ出力する。

---

## 7. 基地局切り替えロジック

### 初期方針

DRL 実装前の baseline として、以下の方策を実装・維持する。

1. 各サイクルで TP / RTT を計測する。
2. RemoteHost または制御ノードで端末満足度を計算する。
3. 端末を「超満足」「満足」「不満足」に分類する。
4. 不満足端末のみを切り替え候補とする。
5. 切り替え後の満足度、調和平均、不満足端末数を記録する。

### 注意すべき観点

- 切り替えた端末だけでなく、切り替え対象外端末の品質変化も記録する。
- 切り替え回数が多すぎる場合は、QoE 悪化や不安定化の原因になり得る。
- 「不満足端末を切り替える」方策は必ずしも最適ではないため、baseline として扱う。

---

## 8. DRL 設計方針

### 状態 State の候補

端末側特徴量:

- 現在の接続先基地局 ID
- 使用アプリケーション種別
- 位置情報
- RSRP または RSSI
- 直近 TP
- 直近 RTT
- 直近の端末満足度 `S_i`
- 直近の分類ラベル

基地局側特徴量:

- 各基地局の接続端末数
- 検査用端末が観測した RTT
- 基地局ごとの平均 TP
- 基地局ごとの不満足端末数
- 直近サイクルからの負荷変化

全体特徴量:

- 端末満足度の調和平均
- 不満足端末数
- 切り替え回数
- サイクル番号

### 行動 Action の候補

- 対象端末の接続維持
- 対象端末を 5G gNB へ切り替え
- 対象端末を Wi-Fi AP1 へ切り替え
- 対象端末を Wi-Fi AP2 へ切り替え

必要に応じて、以下の設計も検討する。

- 1 ステップで 1 端末のみ切り替える
- 1 ステップで複数端末の割り当てを同時に決定する
- 端末選択と基地局選択を分ける

### 報酬 Reward の候補

基本報酬:

```text
reward = H_after - H_before
```

ペナルティ候補:

```text
reward -= alpha * switch_count
reward -= beta  * num_degraded_users
reward -= gamma * large_drop_penalty
```

ここで:

- `H_before`: 切り替え前の調和平均
- `H_after`: 切り替え後の調和平均
- `switch_count`: そのステップでの切り替え回数
- `num_degraded_users`: 満足度が悪化した端末数
- `large_drop_penalty`: 一部端末の大幅な満足度低下へのペナルティ

### アルゴリズム候補

初期検討候補:

- DQN: 離散行動が少ない場合の baseline
- Double DQN: Q 値の過大評価を抑える
- PPO: 状態空間が大きく、方策安定性を重視する場合
- Multi-Agent RL: 端末単位または基地局単位の分散制御を検討する場合

実装時は、最初から複雑な手法に進まず、以下の順で比較する。

1. ルールベース baseline
2. ランダム切り替え baseline
3. DQN または PPO の最小構成
4. 報酬設計を改良した DRL
5. 端末移動・アプリ変動を含む拡張条件

---

## 9. 実験ログ設計

### 必ず保存する項目

各サイクルごとに以下を保存する。

```text
run_id
seed
cycle_id
sim_time
ue_id
current_bs_id
previous_bs_id
app_type
tp_mbps
rtt_ms
satisfaction
satisfaction_class
rsrp_or_rssi
position_x
position_y
num_users_on_bs
harmonic_mean
num_unsatisfied_users
switch_flag
reward
```

### 推奨ファイル形式

- 生ログ: CSV または JSON Lines
- 集計結果: CSV
- 学習データ: Parquet, NumPy, または PyTorch/TensorFlow 形式
- 図: PNG と PDF の両方

### 命名規則

```text
YYYYMMDD_HHMM_<scenario>_<seed>_<memo>.csv
```

例:

```text
20260428_1530_static100_seed001_rulebase.csv
20260428_1600_static100_seed001_dqn_reward_v1.csv
```

---

## 10. 実行コマンド

この節は実際の環境に合わせて更新する。

### ns-3 ビルド

```bash
./ns3 configure --enable-examples
./ns3 build
```

## 11. コーディング規約

### C++ / ns-3

- ns-3 の既存スタイルに合わせる。
- 変数名・関数名は意味が分かる名前にする。
- 研究上重要なパラメータは、コード中に直書きせず引数または設定ファイル化する。
- RTT 計測、接続先変更処理は責務を分ける。
- ログ出力は後処理しやすい形式にする。

### Python

- 解析コードは再実行可能にする。
- 乱数 seed を必ず指定・保存する。
- pandas DataFrame の列名は snake_case に統一する。
- グラフ作成コードでは、軸ラベル・単位・凡例を必ず付ける。
- 学習コードでは、設定・seed・モデル保存先・評価結果をログに残す。

### コメント方針

- 研究上の意味がある処理にはコメントを付ける。
- 単なる構文説明コメントは避ける。
- TODO は以下の形式にする。

```text
// TODO(research): DRL 報酬設計 v2 で切り替えペナルティを追加する
// TODO(exp): 端末数 50/100/200 の比較実験を追加する
```

---

## 12. テスト方針

### 最低限確認すること

- 端末満足度の計算式がアプリ種別ごとに正しい。
- 調和平均の計算で 0 除算が起きない。
- 端末分類の閾値処理が正しい。
- 同一 seed・同一設定で再現可能な結果になる。
- ログに必要な列が揃っている。
- 切り替え前後の基地局 ID が正しく記録される。

## 13. 実験設計ルール

### baseline を必ず用意する

最低限、以下を比較する。

1. 初期接続のまま切り替えなし
2. ランダム切り替え
3. 不満足端末のみを切り替えるルールベース手法
4. DRL 手法

### 評価指標

必ず含める指標:

- 端末満足度の調和平均
- 不満足端末数
- 平均 TP
- 平均 RTT
- 端末満足度の分布
- 切り替え回数
- 切り替え対象外端末の満足度悪化数
- 実行時間または推論時間
- seed 間のばらつき

### 比較時の注意

- 1 回の seed だけで結論を出さない。
- 平均値だけでなく、分散・標準偏差・信頼区間も確認する。
- 改善率を示す場合は、基準手法を明記する。
- 不満足端末数が減っても調和平均が悪化するケースを必ず確認する。

---

## 14. Claude への作業指示

Claude は以下の方針で作業すること。

### 変更前

- まず関連ファイルを読み、既存の設計を把握する。
- 研究目的、評価指標、再現性に影響する変更かを判断する。
- 大きな変更を行う場合は、変更内容を小さな単位に分割する。

### 変更中

- 既存の実験条件を壊さない。
- ns-3.44 前提を崩さない。
- 実験パラメータをハードコードしない。
- ログ形式を変更する場合は、解析スクリプトも同時に更新する。
- 結果を捏造しない。未実行の結果は「未実行」と明記する。

### 変更後

- 実行したコマンドを記録する。
- 成功・失敗・未確認を明確に分けて報告する。
- 研究上の意味を簡潔に説明する。
- 次に実施すべき検証を 1〜3 個示す。

---

## 15. Claude が避けるべきこと

- ns-3 のバージョン変更を勝手に提案・実施しない。
- 実験結果を実行せずに断定しない。
- QoE の改善を平均 TP だけで判断しない。
- 不満足端末数だけを最適化対象にしない。
- 研究目的から外れた大規模リファクタリングを行わない。
- ログ列名を理由なく変更しない。
- seed や設定ファイルを保存しない実験コードを書かない。
- 論文・発表に使う数値を推測で補完しない。

---

## 16. よく使う依頼例

### バグ修正

```text
この ns-3 コードで、基地局切り替え後に previous_bs_id と current_bs_id が正しくログに出ているか確認して、必要なら修正してください。
```

### ログ設計

```text
DRL の報酬設計に使えるように、各サイクルで H_before, H_after, reward, switch_count を CSV に出力する処理を追加してください。
```

### 解析

```text
この CSV から、調和平均、不満足端末数、切り替え回数の推移を seed ごとに集計する Python スクリプトを作成してください。
```

### DRL 実装

```text
現在のログ形式を使って、DQN の最小構成を rl/train.py に実装してください。まずはオフライン学習用で構いません。
```

---

## 17. 現在の優先 TODO

- [ ] DRL 用の状態・行動・報酬を最小構成で固定する。
- [ ] DQN または PPO の baseline 実装を行う。
- [ ] 「不満足端末数」と「調和平均」が乖離するケースを分析する。
- [ ] 端末移動・アプリ変動を導入する前の固定条件で再現性を確認する。

---

## 18. 研究メモ

このリポジトリでは、単に通信品質の平均値を上げることではなく、アプリケーション要求に対する端末満足度を改善し、端末満足度の調和平均を通じて全体 QoE を向上させることを重視する。

特に、基地局切り替えは対象端末だけでなく他端末にも影響を与えるため、DRL の報酬設計・ログ設計・評価指標は「全体最適化」と「再現性」を中心に設計する。（これから）
